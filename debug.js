console.log("Testing validation logic"); const userRules = { required: "This field is required" }; const label = "Required Select"; const required = true; const getDefaultErrorMessages = (label) => ({ required: `${label} is required` }); const applyDefaultMessages = (userRules, label, required) => { const defaults = getDefaultErrorMessages(label); const enhancedRules = {}; if (userRules?.required !== undefined) { if (typeof userRules.required === "boolean") { enhancedRules.required = userRules.required ? defaults.required : false; } else if (typeof userRules.required === "string") { enhancedRules.required = userRules.required; } else { enhancedRules.required = userRules.required; } } else if (required) { enhancedRules.required = defaults.required; } return enhancedRules; }; console.log("Test 1 - explicit rules:", applyDefaultMessages(userRules, label, required)); console.log("Test 2 - only required prop:", applyDefaultMessages(undefined, label, required));
